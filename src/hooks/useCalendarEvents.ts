// Re-export from the shared context provider.
// Components should use useCalendarEventsContext directly,
// but this file is kept for backwards compatibility.
export { useCalendarEventsContext as useCalendarEvents } from '@/contexts/CalendarEventsContext';
